name: Docker Image CI

on:
  push:
    branches: ["main"]

env:
  DOCKER_HUB_USER: xiyang6666
  DOCKER_IMAGE_TAG: xiyang6666/homepage

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
      - uses: actions/checkout@v7

      - run: git fetch --unshallow

      - name: Use Node.js
        uses: actions/setup-node@v7
        with:
          node-version: 24.x

      - name: Setup pnpm
        uses: pnpm/action-setup@v6
        with:
          version: 10

      - run: pnpm install

      - run: pnpm build

      - name: Login to Docker Hub
        uses: docker/login-action@v4
        with:
          username: ${{ env.DOCKER_HUB_USER }}
          password: ${{ secrets.DOCKER_TOKEN }}

      - name: Build Docker image
        run: docker build . --file Dockerfile --tag $DOCKER_IMAGE_TAG:$(git rev-parse --abbrev-ref HEAD)

      - name: Push Docker image
        run: docker push $DOCKER_IMAGE_TAG:$(git rev-parse --abbrev-ref HEAD)
